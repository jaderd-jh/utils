{
  "name": "utils",
  "version": "0.3.4",
  "private": true,
  "description": "jade's utils monorepo",
  "keywords": [
    "jade",
    "utils"
  ],
  "license": "MIT",
  "author": "jade-gjz <jiangang.zhang@jaderd.com>",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build": "pnpm -r build",
    "release": "bumpp -r",
    "check:lint": "oxlint && eslint --cache .",
    "fix:lint": "oxlint --quiet --fix && eslint --cache --fix .",
    "check:fmt": "oxfmt --no-error-on-unmatched-pattern --check",
    "fix:fmt": "oxfmt --no-error-on-unmatched-pattern",
    "publint": "pnpm -r publint",
    "check:type": "tsc --noEmit",
    "check:staged": "lint-staged",
    "pre-commit": "run-p check:type check:staged",
    "init:hooks": "npx simple-git-hooks",
    "prepare": "run-s init:*",
    "update:latest": "pnpm up -i -r --latest && pnpm dedupe --check",
    "update:dedupe": "pnpm dedupe",
    "update:all": "pnpm up && pnpm dedupe",
    "update:nolyfill": "pnpx nolyfill install && pnpm i"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@jhqn/eslint-config": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "bumpp": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-oxlint": "catalog:",
    "jsdom": "catalog:",
    "lint-staged": "catalog:",
    "npm-run-all2": "catalog:",
    "oxfmt": "^0.26.0",
    "oxlint": "catalog:",
    "publint": "catalog:",
    "simple-git-hooks": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vite": "8.0.0-beta.0",
    "vitest": "catalog:"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm pre-commit"
  },
  "lint-staged": {
    "*": [
      "oxfmt --no-error-on-unmatched-pattern",
      "oxlint --quiet --fix",
      "eslint --cache --fix"
    ]
  },
  "packageManager": "pnpm@10.28.1"
}
